> 最新状态请先阅读 [运行修复与手测清单](运行修复与手测清单.md)。下文保留历史交付记录，不代表本次重新验证结果。

# 交付说明(bbhouse-qt 在线功能完整交付 2026-09-17)

WinUI 项目 `Bilibili.History` → Qt6 + QML 的步进迁移。外壳骨架经用户验收通过;本批完成
**全部在线功能**(动态/番剧/特别关注三个在线页 + 剧集模式 + 批量起播),构建 0 error,
真实网络冒烟(动态流/追番桶/UP 种子导入 164 名)全部通过。

## 交付物

- 交付目录:`build/deploy/`(bash scripts/package.sh 一键重建,266MB)
  - `bbhouse-qt.exe` + Qt 运行时 + FluentUI 模块(自托管)+ libmpv-2.dll
- 开发构建:`cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release && cmake --build build`
- 凭据:仓库根 `bilibili.cookie.txt`(已就位,不入库);api-probe 实测有效

## 已完成(openspec 变更 11 个,全部 validate→归档→commit)

| 能力 | 状态 | 验证 |
| --- | --- | --- |
| qt-app-scaffold / app-navigation-shell / settings-ui | ✅ | 用户验收 pass |
| bilibili-api-client(管线+WBI+11 域端点) | ✅ | api-probe PROBE_OK(实数据) |
| local-history-store / history-sync(手动) | ✅ | 自测 18/18;真实同步可用 |
| history-browser-ui(本地历史瀑布流) | ✅ | 用户验收 pass |
| video-playback-window(mpv+自绘面板+弹幕+PGC) | ✅ | 冒烟 |
| online-history-ui / watchlater-ui / card-title-search | ✅ | 冒烟 |
| **dynamics-ui(七档筛选/分区行/aid 去重/右键起播)** | ✅ | 真实动态流冒烟 |
| **bangumi-ui(四档/竖版网格/选集起播)+播放窗口剧集模式** | ✅ | 真实追番桶冒烟 |
| **special-follow-ui(投稿/合集/专栏/管理弹层/playRange)** | ✅ | 种子导入 164 UP |

## 待续(非在线功能,变更文档已建)

- `migrate-local-records-ui`(待播/播放归档——本地 JSON 存储功能,离线性)
- `migrate-scheduled-crawler-service`(定时爬虫 Windows 服务)
- 播放增强延后项:CC 字幕、相邻预解析、内核热替换重建、截图 ≤5MB 压缩

## 本批新增手测项(接交付说明前版清单第 5 条之后)

8. **动态页**:进入默认"全部"档真实拉流;七档切换(切档回顶零网络);视频档分区行解析后出现;重复视频"出现 N 次"角标;视频卡右键起播;搜索(标题/UP)+切页保持;刷新保旧卡
9. **番剧页**:四档切换(番剧/国创=桶1,影视/纪录片=桶2,同桶切档零网络);竖版卡评分/进度;分页;点卡片直接起播;右键详情浮层"播放全集"→剧集模式(选集菜单/列表面板隐藏/连播走分集/末集停止)
10. **特别关注页**:首进自动种子导入(特别分组);头像条切换 UP;投稿=分区+排序+分页;合集=卡片夹→"播放全部"批量入列;专栏=文集卡;管理弹层搜索/勾选/保存(池外成员保持)

发现的问题按 openspec 流程开 fix-* 变更处理。

---

# 追加:macOS 移植与运行修复(fix-macos-port-and-runtime-bugs,2026-09-17)

- macOS(Qt 6.11.2 @ /Users/ziyu/Qt)构建 0 error;`store-test` 18/18;全页冒烟 0 QML 错误。
- 平台修复:zlib 系统库分派、`windows:darkmode=2` 加 Win 守卫、mpv 动态库 macOS 候选路径(已 brew install mpv)。
- FluentUI:FluAcrylic/FluClip 去 Qt5Compat 改 MultiEffect;FluImage 默认异步+解码钳制+重试按钮 ReferenceError 修复。
- 运行修复:动态页封面(strict→lenient,http 封面不再被置空);特别关注页主内容区补 FluScrollBar;特别关注 fc 已定位修复(FluRectangle radius 单值越界 → paint 归一化,真实 cookie 164 UP 实测零崩溃);avif/webp 解码警告消除(按 Qt 插件能力选 CDN 转码格式,不可解码容器转 jpeg)。
- API 核对:13 组端点与 bilibili-API-collect 一致(索引已入项目记忆)。

## 手测清单(macOS,接前版第 10 条之后)

11. **特别关注页**:进入不再 fc;头像条圆形裁切正常;主内容区出现滚动条;管理弹层头像同样圆角;头像(webp)正常显示。
12. **动态页**:封面图片正常加载(此前全灭);无 "不支持的图像格式" 警告;转码图加载失败回退原图仍生效。
13. **全页图片**:卡片封面/头像加载速度与内存占用(大图不再全尺寸解码);加载失败卡片出现"Reload"重试按钮且点击能重新加载。
14. **标题栏毛玻璃**(FluAcrylic 改 MultiEffect 后):窗口标题栏/亚克力区域视觉与改前一致,无黑块/无闪烁。
15. **播放窗口**:mpv 起播(macOS 首个可运行环境):DASH 双流、弹幕、自绘面板、关窗即停音、复开不叠加。
16. **明暗主题**:跟随系统切换正常(macOS 不再强设 windows darkmode 参数)。

## 环境备注

- 本机无 `bilibili.cookie.txt` 限制已解除,cookie 已就位并实测:特别关注种子导入 164 UP、动态/封面/头像真实拉流、播放链路(见下)均正常。
- 若后续希望 avif/webp 原码解码(体积更小),可在 Qt MaintenanceTool 中安装 Qt Image Formats 模块,无需改码(能力探测自动切换)。
- 历史调试说明（2026-09-19 发布隐私修补已移除这两个日志开关，以下仅作历史记录）:`BBHOUSE_MPV_LOG=/tmp/mpv.log`(级别 `BBHOUSE_MPV_LOG_LEVEL`,默认 all=warn);冒烟起播:`BBHOUSE_SMOKE_PLAY_AID=<aid>`。

## 第二轮手测清单(接第 16 条之后)

17. **主窗口(macOS)**:原生标题栏回归(红绿灯可用、拖拽正常、原生圆角);内容区顶部工具行的汉堡按钮可折叠导航、搜索框居中且在卡片页可见;Windows 侧回归确认无变化。
18. **播放窗口(BV1DyY26eEtr 类)**:起播后画面正常出现(不再只有时长无画面);有声;videotoolbox 硬解(顶栏可见编码/解码信息)。
19. **播放窗口(BV1N1tC6zE7B 类)**:不再弹"API 404";正常起播。
20. **续播**:播一部分关掉再进,从上次位置续播(loadfile start 参数修复后生效)。
21. **DASH 音画同步**:外挂音轨场景音画同步、切清晰度后音轨保持。
