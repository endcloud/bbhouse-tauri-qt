## ADDED Requirements

### Requirement: 流行页面接线
导航 SHALL 以“流行”作为页面名称，在动态之后提供入口，首次进入按需创建页面，切走保留实例和独立搜索状态。

#### Scenario: 导航与返回
- **WHEN** 用户进入流行，切到其他页面或作者空间后返回
- **THEN** 恢复流行页的页签、期数/榜单选择、搜索、分页和滚动，不无条件重新请求。
