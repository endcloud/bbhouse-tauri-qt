# Proposal: migrate-special-follow-ui

特别关注页(契约: special-follow-ui 规格)。本地 UP 列表(JSON 快照+特别分组一次性种子导入)+头像条;单页三档:投稿(ArcSearchApi WBI 30/页分页+分区 ToggleButton 行+排序三档)/合集(SeasonsApi 合集卡+批量起播 PlayRange 去重保位)/专栏(ArticleApi 文集卡本地分页);管理弹层(服务端搜索 400ms 防抖 pn≤5+池外成员保持合并写回)。
