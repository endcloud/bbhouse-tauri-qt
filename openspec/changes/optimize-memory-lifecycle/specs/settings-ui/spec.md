## ADDED Requirements

### Requirement: 后台页面保留时间
设置 SHALL 提供后台页面保留时间 1/5/10/30 分钟，默认 5 分钟，持久化且立即生效。非法配置 SHALL 回退默认值；切换设置 SHALL 从变更时重新计算已在后台页面的保留期限。

#### Scenario: 调整保留时间
- **WHEN** 用户选择 1 分钟
- **THEN** 后台页面重新按 1 分钟计时，下次启动沿用此选择
